<%= form_for(actor_skill) do |f| %>
  <% if actor_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actor_skill.errors.count, "error") %> prohibited this actor_skill from being saved:</h2>

      <ul>
      <% actor_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :special_skill_id %>
    <%= f.number_field :special_skill_id %>
  </div>

  <div class="field">
    <%= f.label :actor_id %>
    <%= f.number_field :actor_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
