<%= form_for(actor_height) do |f| %>
  <% if actor_height.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actor_height.errors.count, "error") %> prohibited this actor_height from being saved:</h2>

      <ul>
      <% actor_height.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :height %>
    <%= f.text_field :height %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
