<%= form_for(actor_form) do |f| %>
  <% if actor_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actor_form.errors.count, "error") %> prohibited this actor_form from being saved:</h2>

      <ul>
      <% actor_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :other_forms_link_id %>
    <%= f.number_field :other_forms_link_id %>
  </div>

  <div class="field">
    <%= f.label :actor_id %>
    <%= f.number_field :actor_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
