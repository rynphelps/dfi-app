<%= form_for(picture_link) do |f| %>
  <% if picture_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(picture_link.errors.count, "error") %> prohibited this picture_link from being saved:</h2>

      <ul>
      <% picture_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :photo %>
    <%= f.text_field :photo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
