<%= form_for(special_skill) do |f| %>
  <% if special_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(special_skill.errors.count, "error") %> prohibited this special_skill from being saved:</h2>

      <ul>
      <% special_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :skill %>
    <%= f.text_field :skill %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
