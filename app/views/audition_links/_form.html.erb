<%= form_for(audition_link) do |f| %>
  <% if audition_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(audition_link.errors.count, "error") %> prohibited this audition_link from being saved:</h2>

      <ul>
      <% audition_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :audition %>
    <%= f.text_field :audition %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
