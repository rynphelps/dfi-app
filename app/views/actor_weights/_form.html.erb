<%= form_for(actor_weight) do |f| %>
  <% if actor_weight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actor_weight.errors.count, "error") %> prohibited this actor_weight from being saved:</h2>

      <ul>
      <% actor_weight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :weight %>
    <%= f.text_field :weight %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
